import { GmailDirective } from './gmail.directive';

describe('GmailDirective', () => {
  it('should create an instance', () => {
    const directive = new GmailDirective();
    expect(directive).toBeTruthy();
  });
});
